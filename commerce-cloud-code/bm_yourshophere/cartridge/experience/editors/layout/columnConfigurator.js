/**
 * Init for the column configurator custom editor
 *
 * Initialises the custom attribute editor with server side information.
 * This editor doesn't need any server-side configuration.
 */
module.exports.init = function () {
    // No server-side initialization needed for this editor
    // All configuration is handled client-side
};
